import '../tap_choice/tap_choice_game.dart';

/// PLACEHOLDER: temporarily plays as a TapChoice round so the path is
/// playable end-to-end while the real PatternCompleteGame engine is implemented.
class PatternCompleteGame extends TapChoiceGame {
  PatternCompleteGame(super.gameContext);
}
