/// Milo's animation states. Placeholder art is drawn procedurally by
/// MiloPainter; the same states map 1:1 onto sprite-sheet rows when real
/// artwork arrives (see ASSET_GUIDE.md).
enum MiloState {
  idle,
  talking,
  pointing,
  happy,
  laughing,
  surprised,
  dancing,
  sleepy,
}
